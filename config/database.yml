common: &common
  adapter: postgresql
  username: geoff
  passwore: mass2742 # from psql setup, see Postgresql

development:
  adapter: postgresql
  username: geoff
  database: rails_development

test: &test
  <<: *common
  database: myapp_test

production:
  <<: *common
  database: martha_prod


cucumber:
  <<: *test
